<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.yedam.app.board.mapper.BoardMapper">
  <!-- 전체조회 -->
  <select id="selectBoardList" resultType="BoardVO">
  SELECT bno, 
  		title, 
  		contents, 
  		writer, 
  		regdate, 
  		image 
  	FROM tbl_board_01
  	order by bno
  </select>
  
  <!-- 단건조회 -->
  <select id="selectBoard" resultType ="BoardVO">
  SELECT bno, 
  		title, 
  		contents, 
  		writer, 
  		regdate, 
  		image 
  from tbl_board_01 
  where bno=#{bno}
  </select>
  <!-- 등록 -->
  <insert id="insertBoard" parameterType ="BoardVO">
  <selectKey keyProperty="bno" resultType="integer"
			order="BEFORE">SELECT NVL(MAX(bno),1000)+1 FROM tbl_board_01</selectKey> 
  INSERT INTO tbl_board_01(bno, title, contents, writer, image)VALUES(#{bno}, #{title}, #{contents}, #{writer}, #{image})
  </insert>
  <!-- 수정 -->
  <update id="updateBoard" parameterType = "BoardVO"> 
  UPDATE tbl_board_01 SET title=#{title}, contents=#{contents},writer=#{writer},updateDate=#{updateDate}, image=#{image}
  WHERE bno = #{bno}
  </update>
  <!-- 삭제 -->
  <delete id = "deleteBoard" parameterType = "int">
  DELETE FROM tbl_board_01
  WHERE bno = #{boardNO}
  </delete>
  </mapper>